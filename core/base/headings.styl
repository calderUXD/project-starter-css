/*------------------------------------*\
    !-Headings
\*------------------------------------*/

/**
 *  
 * Extract site wide headings and provide accompanying classes so that the styles
 * are more portable. thanks to Nicole Sulivan a.k.a stubornella
 * http://www.stubbornella.org/content/2011/09/06/style-headings-using-html5-sections/
 *
 */

h1, .h-1
	calc-font-size($h1-size)

h2, .h-2
	calc-font-size($h2-size)

h3, .h-3
	calc-font-size($h3-size)

h4, .h-4
	calc-font-size($h4-size)

h5, .h-5
	calc-font-size($h5-size)

h6, .h-6
	calc-font-size($h6-size)


/**
 *  
 * Target headings or headings in groupd to remove bottom Margin. When you want to stack
 * headers on top of each other.
 * <h1 class="h-0">Main Heading</a>
 * <h2 class="h-0">Subheading</a>
 *
 */

hgroup
	.h-0
		margin-bottom:0
