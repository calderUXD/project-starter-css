/*------------------------------------*\
    !-MAIN
\*------------------------------------*/

html
    font:unit(($base-font-size/16px) * 1, em) / $line-height-ratio sans-serif
    overflow-y:scroll
    min-height:100%